/**
 * Check that checkbox found by `id` is `checked` (or unchecked for `false`)
 *
 * @example
 * ```javascript
 * checkboxValue('includeExpired', true)
 * ```
 * @category Checkbox
 * */
export declare function checkCheckbox(id: string, checked: boolean): void;
export declare function getCheckboxValue(id: string): boolean;
