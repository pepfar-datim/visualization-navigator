import React from 'react';
import { CustomData, CustomLinkOptions } from '../../links';
interface CustomProviderInput {
    children: React.ReactNode;
    data: CustomData;
    options?: CustomLinkOptions;
    queryClientOptions?: any;
}
export declare const CustomDataProvider: ({ children, data, options, queryClientOptions, }: CustomProviderInput) => JSX.Element;
export {};
