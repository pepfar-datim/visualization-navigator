"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('reordering of items is enabled', () => {// no op
});
(0, _steps.Given)('the selected list has three items', () => {
  cy.visitStory('Transfer Reorder Buttons', 'Has Some Selected');
});
(0, _steps.Given)('the selected list has some items', () => {
  cy.visitStory('Transfer Reorder Buttons', 'Has Some Selected');
});
(0, _steps.Given)('the {int}. item is highlighted', previousPisition => {
  const index = previousPisition - 1;
  cy.get('{transfer-pickedoptions} {transferoption}').eq(index).as('previous').click();
});
(0, _steps.Given)('no items are highlighted in the list', () => {
  cy.get('{transfer-pickedoptions} {transferoption}.highlighted').should('not.exist');
});
(0, _steps.Given)('more than one item is highlighted in the list', () => {
  cy.get('{transfer-pickedoptions} {transferoption}').filter(index => index < 2).each($option => cy.wrap($option).clickWith('ctrl'));
});
(0, _steps.When)("the user clicks the 'move up' button", () => {
  // force, so we click disabled buttons
  cy.get('{transfer-reorderingactions-buttonmoveup}').click({
    force: true
  });
});
(0, _steps.When)("the user clicks the 'move down' button", () => {
  // force, so we click disabled buttons
  cy.get('{transfer-reorderingactions-buttonmovedown}').click({
    force: true
  });
});
(0, _steps.Then)('the highlighted item should be moved to the {int}. place', next => {
  const index = next - 1;
  cy.get('@previous').invoke('index').should('equal', index);
});
(0, _steps.Then)("the 'move up' and 'move down' buttons should be disabled", () => {
  cy.get('{transfer-reorderingactions-buttonmoveup}').should('have.attr', 'disabled');
  cy.get('{transfer-reorderingactions-buttonmovedown}').should('have.attr', 'disabled');
});