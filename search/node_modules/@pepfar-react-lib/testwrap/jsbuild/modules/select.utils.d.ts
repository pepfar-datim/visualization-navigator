/**
 * Select `value` in select element found by `id`
 *
 * @example
 * ```javascript
 * select('pizzaDough','newYorkSyle')
 * ```
 * @category Select
 * */
export declare function select(id: string, value: string): void;
export declare function selectById(id: string, value: string): void;
/**
 * Check that select element found by `id` has value `value`
 *
 * @example
 * ```javascript
 * checkSelectValue('pizzaDough','newYorkSyle')
 * ```
 * @category Select
 * */
export declare function checkSelectValue(id: string, value: string): void;
